/**
 * Seed de templates e questões: DESATIVADO.
 * O admin cria templates e questões manualmente na aplicação e associa ao modelo desejado.
 * Para popular dados iniciais, use a interface de administração (Templates / Questões).
 *
 * Mantido apenas para referência ou execução manual futura (vazio).
 */
export async function seedTemplates(): Promise<void> {
  // Sem seed: templates e questões são criados pelo admin na aplicação.
  return;
}
